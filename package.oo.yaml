version: 0.0.4
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: poetry add pandas
  bootstrap: |
    npm install
    poetry install --no-root
icon: https://package-assets.oomol.com/packages/image-to-icon/0.0.1/files/package/icon.png
name: image-to-icon
description: image to icns & ico
displayName: Image To Icon
releaseNotes: update version
repository: https://github.com/oomol-flows/image-to-icon
